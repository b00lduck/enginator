#include "vss_stdinc.h"
#include "cvss.h"
#include "cvssreplay.h"
/*

CVSSReplay::CVSSReplay(HWND hWnd) {
  myVSS = new CVSS();
  myVSS->Init(hWnd);
}

void CVSSReplay::Play() {
  myVSS->Play();
} 

void CVSSReplay::Stop() {
  myVSS->Stop();
}

void CVSSReplay::RenderTrigger() {
  myVSS->RenderTrigger();
}

CVSSReplay::~CVSSReplay() {
  myVSS->~CVSS();
}


*/